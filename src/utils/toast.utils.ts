import { ToastOptions } from "react-hot-toast";

export const darkToastOptions: ToastOptions = {
    style: {
        background: "#000",
        color: "#fff",
        border: '2px solid rgb(100,100,100)'
    },
}